import type { MetadataRoute } from "next";
import { posts, projects, services } from "./content";
export default function sitemap(): MetadataRoute.Sitemap { const base = "https://khaithien.vn"; const staticRoutes = ["", "/gioi-thieu", "/dich-vu", "/du-an", "/insights", "/lien-he", "/gui-brief", "/chinh-sach-bao-mat", "/dieu-khoan-su-dung"]; return [...staticRoutes.map((route) => ({ url: `${base}${route}`, changeFrequency: route === "" ? "weekly" as const : "monthly" as const, priority: route === "" ? 1 : .7 })), ...services.map((item) => ({ url: `${base}/dich-vu/${item.slug}`, changeFrequency: "monthly" as const, priority: .8 })), ...projects.map((item) => ({ url: `${base}/du-an/${item.slug}`, changeFrequency: "monthly" as const, priority: .7 })), ...posts.map((item) => ({ url: `${base}/insights/${item.slug}`, changeFrequency: "monthly" as const, priority: .6 }))]; }
